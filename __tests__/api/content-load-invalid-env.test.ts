/**
 * Removed: this file previously had no tests and caused Jest to fail.
 * API env/edge cases are fully covered in:
 * - __tests__/api/content-load.test.ts
 */
it("noop", () => {
  expect(true).toBe(true);
});